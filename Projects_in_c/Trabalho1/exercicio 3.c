#include <stdio.h>

int main() {
    int codigoEstado;
    float pesoCarga;
    int codigoCarga;

    printf("Digite o cOdigo do estado de origem da carga (1 a 5): ");
    scanf("%d", &codigoestado);

    printf("Digite o peso da carga em toneladas: ");
    scanf("%f", &pesocarga);

    printf("Digite o codigo da carga (10 a 40): ");
    scanf("%d", &codigoCc
    printf("Codigo do estado de origem: %d\n", codigoEstado);
    printf("Peso da carga: %.2f toneladas\n", pesoCarga);
    printf("Codigo da carga: %d\n", codigoCarga);

    return 0;

    system ("pause");
}
